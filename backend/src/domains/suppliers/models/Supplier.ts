export interface Supplier {
  id: string;
  businessId: string;
  name: string;
  phone?: string;
  momoPhone?: string;
  bankAccount?: string;
  currency: string;
  countryCode: string;
  notes?: string;
  createdAt: string;
  updatedAt: string;
}
