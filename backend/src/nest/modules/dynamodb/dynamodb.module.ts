/**
 * Re-exports from @tkhtech/nest-dynamodb.
 * All existing import paths across Kaba continue to work unchanged.
 * Source of truth is now the shared platform package.
 */
export { DynamoDBModule, DYNAMODB_CLIENT, DYNAMODB_DOC_CLIENT } from '@tkhtech/nest-dynamodb';
